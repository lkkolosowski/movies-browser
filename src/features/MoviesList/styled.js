import styled from "styled-components";

export const Title = styled.h1`
  margin: 0 0 24px;
  font-size: 36px;
  font-weight: 600;
  line-height: 1.2;
  color: ${({ theme }) => theme.color.woodsmoke};
`;
